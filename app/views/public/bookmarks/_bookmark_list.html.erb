<ul class="list-group list-group-flush">
  <% bookmarks.each do |bookmark| %>
    <li class="list-group-item p-3">
      <% if bookmark.topic.end_user.active_for_authentication? %>
          <p class="h5 my-2"><%= link_to bookmark.topic.title.truncate(35), community_topic_path(bookmark.topic.community, bookmark.topic) %></p>
      <% else %>
          <p class="h5 my-2"><%= bookmark.topic.title.truncate(35) %></p><small>※このトピックは投稿したユーザーが利用停止中のため、閲覧できません</small>
      <% end %>
      <div class="row justify-content-end mr-1">
        <small class="mr-2"><%= link_to bookmark.topic.community.name, community_path(bookmark.topic.community) %></small>
        <small class="ml-2" id="bookmark-buttons-<%= bookmark.topic.id %>"><%= render 'public/bookmarks/bookmark_btn', topic: bookmark.topic %></small>
        <small class="topic-comments-count ml-2"><%= render 'public/topic_comments/comment_count', topic: bookmark.topic %></small>
      </div>
    </li>
  <% end %>
</ul>